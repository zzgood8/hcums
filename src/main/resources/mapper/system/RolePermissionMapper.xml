<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zbx.hcums.system.dao.RolePermissionMapper">

    <select id="exists" resultType="java.lang.Boolean">
        select exists(select id from A_role_permission where role_id = #{roleId} and permission_id = #{permissionId})
    </select>

    <delete id="delete" parameterType="java.lang.Long">
        delete
        from A_role_permission
        where role_id = #{roleId}
          and permission_id = #{permissionId}
    </delete>

    <delete id="deleteByPermissionId">
        delete
        from A_role_permission
        where permission_id = #{permissionId}
    </delete>
    <delete id="deleteByRoleId">
        delete
        from A_role_permission
        where role_id = #{roleId}
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.zbx.hcums.system.entity.po.RolePermissionPo">
        insert into A_role_permission
            (role_id, permission_id, create_time, create_by)
        values (#{roleId}, #{permissionId}, #{createTime}, #{createBy})
    </insert>
</mapper>
